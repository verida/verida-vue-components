# Basic set up for npm

version: 2
updates:
  # Maintain dependencies for npm
  - package-ecosystem: 'npm'
    directory: '/components/account'
    schedule:
      interval: 'weekly'
      day: 'tuesday'
      allow: 
      - dependency-name: "@verida/*"
